# 15秒分镜提示词 · Screenplay-aware Storyboard Skill

将剧本、人物设定和场景参考转成**可直接复制的真人短剧视频提示词**。技能以 15 秒为一组，先理解剧本整体，再核对多人站位和镜头连续性，最后输出统一提示词与逐秒分镜。

> 分镜模块负责编写与核查提示词；新增的三视图模块可在可用的图像工具中直接生成角色资产图。提示词和参考图可以降低身份漂移，但不能保证模型每次都生成完全相同的面孔或准确还原原图看不到的侧背面。

## 适用场景与核心定位

这个 Skill 的核心使用场景，是让**没有影视、摄影、分镜或导演基础的用户，也能通过简单的口头描述，把脑海里的画面转换成具有完整动作逻辑、镜头语言和分镜思维的 AI 视频提示词**。

你不需要先学会景别、轴线、焦段、机位、调度、动作连续性或剪辑术语。可以直接像和导演沟通一样，用自然语言告诉 AI：

```text
这个男人发现对面的车准备加速离开，他一下慌了，立刻追上去，最后抢到前面横车把她拦住，要拍得紧张一点。
```

Skill 会把这类简单描述继续拆解为可执行的镜头逻辑，包括：

- 人物动作如何从“发现 → 反应 → 行动 → 接触或结果”连续发生，避免瞬移和动作断裂；
- 应该使用什么景别、机位、焦距、构图、前景遮挡和运镜来表现动作与情绪；
- 多个人物在复杂场景中的站位、视线、运动方向、摄影轴线和相互反应；
- 一个复杂动作应该如何拆成多个镜头，以及每个镜头应该持续几秒；
- 哪个动作适合做特写、反应镜头、过肩镜头、跟拍、推轨、环绕、匹配剪辑或其他镜头语言；
- 如何将用户只描述了“感觉”的内容，转化成可以直接用于 AI 视频生成的具体画面指令；
- 如何在多组 15 秒镜头之间保持人物、道具、车辆、场景、动作和情绪连续。

因此，它既可以给有影视经验的人作为**分镜与连续性辅助工具**，也特别适合第一次接触 AI 视频制作、不会写专业提示词、不会画机位图、甚至不知道镜头术语的用户。用户只负责描述“我想看到什么”，Skill 负责进一步完成“这个画面应该怎么拍、人物应该怎么动、镜头应该怎么接”。

**零基础也可以直接上手。** 最简单的使用方式就是：描述人物是谁、在哪里、发生什么、你希望观众感受到什么；其余复杂的人物动作链、镜头语言和分镜逻辑可以交给 Skill 继续展开。

## 作者注释

本技能的标准注释为：

```html
<!-- 谢谢你用我的技能 我叫瑞 -->
```

主 `SKILL.md` 已包含该注释。若技能从本地技能目录调用，并且运行环境允许写入本地文件，调用前应运行：

```bash
python scripts/ensure_local_notice.py
```

脚本会检查本地 `SKILL.md`，缺少时自动添加该注释；已有时不会重复写入。只读环境则跳过写入，不影响技能继续工作。

## 能做什么

| 功能 | 具体行为 | 输出 |
| --- | --- | --- |
| 整剧理解 | 阅读一次上传或分批提供的剧本；区分已读与未读场次、版本和故事发生顺序 | 场次索引、人物关系、戏剧目标与情绪走向 |
| 连续性记录 | 跟踪人物外貌、服装、知情状态、伤痕、道具持有者、日夜天气、光源及每场进出状态 | 可供后续场次复用的项目资料 |
| 多人预排 | 以门窗、餐桌、车辆等实体为坐标，确认站位、运动路线、视线、摄影轴线、遮挡和配角反应 | 清楚的空间与动作链 |
| 15 秒分镜 | 按秒拆镜，写景别、机位、焦距、人物位置、微表情、连续动作、运镜、光源和切点 | 每组 0–15 秒完整提示词 |
| 多组衔接 | 将较长场次拆为多组，核对前组末帧与后组首帧 | 连续场次的多组提示词 |
| 穿帮预检 | 核对人数、进出画、道具换手、视线、光向、对白时长和人物知情范围 | 修正后的分镜与必要的待确认点 |
| 人物三视图资产 | 查看本轮上传的单人人物图，锁定可见脸部和衣饰，保守补全不可见部位；统一柔和哑缎肤质 | 左大头照＋右正面、严格 90° 侧面、背面全身图，或可复制提示词 |

## 如何使用

在支持个人技能的 ChatGPT/Codex 环境中启用该技能，或将 `write-15-second-storyboards/` 作为技能目录导入；之后可以用 `$write-15-second-storyboards` 显式调用。仅复制 README 不会安装技能。

三视图还可以通过独立技能 [`character-turnaround-from-image/`](character-turnaround-from-image/SKILL.md) 调用；两处采用相同的生成规则。把人物图附在同一条请求中：

```text
调用 $character-turnaround-from-image，用我刚上传的单人人物图直接生成三视图。
左边面部大头照，右边正面、严格90度侧面和背面全身；保留原脸和服装，柔肤、眼睛清楚。
```

若只要文字，将「直接生成三视图」改为「给我可复制的三视图主提示词和负面词」。也可以调用 `$write-15-second-storyboards` 并说「使用人物三视图模块」，随后把确定的角色资产用于分镜连续性。参考图中看不到的鞋子和背面细节属于推断；不要把参考图或私人剧本提交到公共仓库。

### 0. 零基础最快用法：直接说你想看到什么

不需要使用摄影术语，可以直接口头描述。例如：

```text
调用 $write-15-second-storyboards。
女生一个人在餐桌上吃饭，听到门响以后停下来，慢慢抬头看向门口。
我想要一种表面很平静，但是她其实已经开始紧张的感觉，镜头高级一点。
```

Skill 会进一步决定合理的景别变化、人物微动作、视线变化、动作先后顺序、镜头位置、焦距、运镜、光源关系和切镜节点，再整理成可直接用于 AI 视频生成的分镜提示词。

### 1. 导入剧本

上传 TXT、PDF 或 DOCX 剧本，或分集粘贴。示例请求：

```text
使用 $write-15-second-storyboards 阅读这份完整剧本。
先建立场次索引、人物与场景连续性资料，列出你确实读到的集数和场次。
暂时不要生成分镜；发现剧本版本冲突或关键设定缺失时标明。
```

技能不会自动把整部剧本写进自身文件。需要跨会话沿用时，让助手建立可持续读取的项目资料，下次工作时提供或指定最新剧本和资料。不要在公开仓库提交未获授权的剧本、人物参考图或私密项目资料。

### 2. 生成指定场次

```text
使用 $write-15-second-storyboards 生成第 3 集第 5 场。
参考已整理的剧本与人物设定，按连续的 15 秒组输出。
统一提示词保证场景和光影；分镜按秒写景别、机位、主体站位、动作链和反应。
重点核查三人站位、谁拿着钥匙、门的位置和组与组之间的末帧衔接。
```

### 3. 修改与复核

```text
第二组第 4 镜的人物从桌边瞬移到门口了。
只修改这段及必要的前后衔接，保留其他镜头与台词。
重新核对动作所需时间、摄影轴线和道具归属。
```

## 标准化文件结构

```text
write-15-second-storyboards/
├── SKILL.md                          # 主技能定义、触发条件、工作流、输出规范
├── agents/
│   └── openai.yaml                  # ChatGPT / Codex 技能界面与调用配置
├── assets/
│   └── icon.svg                     # 技能图标
├── references/
│   ├── script-continuity.md         # 整剧理解、多人连续性与穿帮核查流程
│   └── turnaround-workflow.md        # 人物身份、柔肤质感和正侧背生成模板
└── scripts/
    └── ensure_local_notice.py       # 本地调用前确保作者注释存在

examples/
└── dinner-scene.md                  # 仓库级虚构演示，不属于运行时必需文件

character-turnaround-from-image/
├── SKILL.md                          # 可单独调用的三视图技能
├── agents/openai.yaml
├── references/turnaround-workflow.md # 与分镜技能中的模块规则一致
└── scripts/ensure_local_notice.py
```

运行时核心内容统一收纳在 `write-15-second-storyboards/` 技能目录中；仓库根目录只保留 README 和演示资料，避免把运行时依赖与说明文档混在一起。

## 操作演示：三人餐桌戏

以下是**虚构示例**，不包含用户的实际剧本或图片。完整演示见 [examples/dinner-scene.md](examples/dinner-scene.md)。

**输入梗概：** 夜晚，A 坐在餐桌近门一侧，B 坐在对面，C 从玄关走来并把一把钥匙放在两人之间。A 先看钥匙，再看 C；B 一直坐着，尚不知道钥匙的来历。

**连续性锚点：** 玄关是唯一入口；A 与 B 不换座；C 从玄关步行到桌边；钥匙先在 C 右手，放下后位于桌面两人之间。A 的视线先落钥匙后落 C；B 只对自己看见的动作作反应。

**输出示意：**

```text
统一提示词：9:16 夜内，餐桌位于客厅与玄关之间。暖色吊灯照亮桌面与人物面部，窗外深蓝仅作背景。三人服装、餐具和椅子位置在全组保持一致。无音乐、BGM和字幕，保留脚步、钥匙落桌和指定对白的同期声。

0–4秒【中景｜桌旁朝玄关的斜侧机位｜35mm，T2.8｜固定】A 坐在靠门一侧，B 坐在对面。C 从玄关走向餐桌，右手握着钥匙；A 听到脚步抬眼，B 保持坐姿。切在 C 到达桌边。

4–8秒【双人中近景｜同一轴线侧｜50mm，T2.2｜轻推】C 停在桌边，右手从身侧移到桌面，将钥匙放在 A 与 B 之间，手指松开后撤回。A 的目光跟随钥匙；B 随动作低头，不提前表现出已知内情。

8–12秒【A 近景｜桌边三分之四侧面｜75mm，T2.0｜固定】A 先看桌上的钥匙，停半秒，再抬眼看 C；肩膀微微绷紧，没有站起。桌面一角仍可见钥匙，位置不变。

12–15秒【三人中景｜沿用首镜轴线侧｜50mm，T2.8｜固定】A 仍坐在靠门一侧，C 站在桌边，B 坐在对面并看向钥匙。以三人的新状态结束，下一组从此站位接起。
```

## 边界

- 15 秒是单组长度，长场次可生成多组；长对白不会为了塞入 15 秒而被悄悄删去。
- 参考图只约束可见空间；未展示的背后结构必须标为待确认或合理假设。
- 输出前检查文本逻辑；实际视频仍需人工核查面容、手部、物件、空间与剪辑连续性。
